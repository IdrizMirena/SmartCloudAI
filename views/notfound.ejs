<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>UCFM Login</title>
</head>

<body>
    <style>
        body {
            background-image: url('bgg.jpg');
            background-size: cover;
            background-position: center;
            position: relative;
            backdrop-filter: blur(10px);
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            font-family: 'Source Sans Pro', sans-serif;
            font-size: 18px;
            font-weight: 400;
            text-transform: uppercase;
            letter-spacing: .1em
        }

        p {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%);
        }

        a {
            display: block;
            color: white;
            text-decoration: none;
            position: relative;
        }

        a::after {
            content: "";
            background: rgb(192, 0, 0);
            mix-blend-mode: exclusion;
            width: calc(100% + 20px);
            height: 0;
            position: absolute;
            color: black;
            bottom: -4px;
            left: -10px;
            transition: all .3s cubic-bezier(0.445, 0.05, 0.55, 0.95);
        }

        a:hover::after {
            height: calc(100% + 8px)
        }

        /* The End */

        .note {
            top: auto;
            bottom: 0;
            color: #dd0000;
            font-size: 13px;
            text-transform: none;
            font-weight: 300;
            text-align: center;
            padding: 10px;
            width: 100%;
            max-width: 500px;
        }
    </style>
    <link href="https://fonts.googleapis.com/css?family=Source+Sans+Pro:300,400" rel="stylesheet">

    <h1 style="color: red; display: flex; justify-content: center;padding: 280px;">User Not Found , Create Account !</h1>
    <p><a href="/login">Create Account</a></p>


</body>

</html>